let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_10b‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_10b‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_10b‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_10b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_10b‍.w("./api",[["default",["api"],function(v){api=v}]]);_10b‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  // mongo 6 버전부터는 useNewUrlParser/useFindAndModify 지원 x
  // .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _10b‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _10b‍.g.console.log('Listening to port %d', port);
});
